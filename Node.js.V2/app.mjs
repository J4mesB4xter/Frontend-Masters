// `we can import modules by calling them into the file as constants; this module is from index.js and was created as an example`

// const action = require('./index.js')

//importing through ES6 would look more like this:
import { action } from './index'